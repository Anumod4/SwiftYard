meta {
  name: Save Resource
  type: http
  seq: 2
}

post {
  url: "http://localhost:4000/api/resources/save"
  auth: bearer
}

body {
  type: json
  json: {
    "name": "Dock 1",
    "type": "Dock",
    "status": "Available",
    "operationMode": "Standard"
  }
}

headers {
  X-Facility-ID: {{facilityId}}
}
