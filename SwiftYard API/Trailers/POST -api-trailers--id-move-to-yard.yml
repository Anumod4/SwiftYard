info:
  name: POST /api/trailers/:id/move-to-yard
  type: http
  seq: 8

http:
  method: POST
  url: "{{API_BASE_URL}}/api/trailers/:id/move-to-yard"
  headers:
    - name: Authorization
      value: Bearer {{JWT_TOKEN}}
    - name: X-Facility-ID
      value: "{{FACILITY_ID}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      { 
          
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
