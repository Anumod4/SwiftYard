info:
  name: DELETE /api/appointments/:id
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{API_BASE_URL}}/api/appointments/:id"
  headers:
    - name: Authorization
      value: Bearer {{JWT_TOKEN}}
    - name: X-Facility-ID
      value: "{{FACILITY_ID}}"
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
