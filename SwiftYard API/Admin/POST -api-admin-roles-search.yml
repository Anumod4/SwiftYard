info:
  name: POST /api/admin/roles/search
  type: http
  seq: 9

http:
  method: POST
  url: "{{API_BASE_URL}}/api/admin/roles/search"
  headers:
    - name: Authorization
      value: Bearer {{JWT_TOKEN}}
    - name: X-Facility-ID
      value: "{{FACILITY_ID}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      { 
          
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
