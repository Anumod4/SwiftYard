info:
  name: DELETE /api/admin/roles/:id
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{API_BASE_URL}}/api/admin/roles/:id"
  headers:
    - name: Authorization
      value: Bearer {{JWT_TOKEN}}
    - name: X-Facility-ID
      value: "{{FACILITY_ID}}"
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
