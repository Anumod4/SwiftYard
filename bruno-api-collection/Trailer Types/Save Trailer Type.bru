meta {
  name: Save Trailer Type
  type: http
  seq: 2
}

post {
  url: "http://localhost:4000/api/trailer-types/save"
  auth: bearer
}

body {
  type: json
  json: {
    "name": "Standard",
    "defaultDuration": 60,
    "processTimePerPallet": 5
  }
}

headers {
  X-Facility-ID: {{facilityId}}
}
