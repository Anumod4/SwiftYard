meta {
  name: Save Settings
  type: http
  seq: 2
}

post {
  url: "http://localhost:4000/api/settings/save"
  auth: bearer
}

body {
  type: json
  json: {
    "appointmentBufferTime": 15,
    "defaultDuration": 60
  }
}

headers {
  X-Facility-ID: {{facilityId}}
}
